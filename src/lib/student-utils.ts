export function getStudentDisplayNameFromId(studentId: string): string {
  // Placeholder: Once backend is integrated, look up student name by ID.
  // For now, we simply return the ID itself as the display string.
  return studentId;
}

